import { GoogleGenAI } from '@google/genai'; import dotenv from 'dotenv'; dotenv.config({ path: 'server/.env' }); const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY }); async function test() { try { const res = await ai.models.generateImages({ model: 'gemini-2.5-flash-image', prompt: 'a cute cat', config: { numberOfImages: 1, outputMimeType: 'image/jpeg' } }); console.log(res.generatedImages ? 'Success' : 'Failed'); } catch(e) { console.error(e) } } test();  
