import { GoogleGenAI } from '@google/genai'; import dotenv from 'dotenv'; dotenv.config({ path: 'server/.env' }); const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY }); async function test() { try { const res = await ai.models.generateContent({ model: 'gemini-2.5-flash-image', contents: 'a cute cat', config: { responseModalities: ['IMAGE'] } }); console.log(res.candidates[0].content.parts[0].inlineData.mimeType ? 'Success' : 'Failed'); } catch(e) { console.error(e) } } test();  
